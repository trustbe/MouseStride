name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Extract version
        id: version
        run: echo "tag=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"

      - name: Import signing certificates
        env:
          MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
          MACOS_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
          MACOS_INSTALLER_CERTIFICATE: ${{ secrets.MACOS_INSTALLER_CERTIFICATE }}
          MACOS_INSTALLER_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_INSTALLER_CERTIFICATE_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          echo "$MACOS_CERTIFICATE" | base64 --decode > /tmp/app.p12
          echo "$MACOS_INSTALLER_CERTIFICATE" | base64 --decode > /tmp/installer.p12
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security set-keychain-settings -t 3600 -u build.keychain
          security import /tmp/app.p12 -k build.keychain \
            -P "$MACOS_CERTIFICATE_PASSWORD" \
            -T /usr/bin/codesign
          security import /tmp/installer.p12 -k build.keychain \
            -P "$MACOS_INSTALLER_CERTIFICATE_PASSWORD" \
            -T /usr/bin/productsign -T /usr/bin/pkgbuild
          security set-key-partition-list -S apple-tool:,apple:,codesign:,productsign:,pkgbuild: \
            -s -k "$KEYCHAIN_PASSWORD" build.keychain
          rm /tmp/app.p12 /tmp/installer.p12
          security find-identity -v build.keychain

      - name: Build, sign, notarize, staple, zip
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
        run: make zip VERSION=${{ steps.version.outputs.tag }}

      - name: Build signed installer pkg
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
        run: make pkg VERSION=${{ steps.version.outputs.tag }}

      - name: Verify universal binary
        run: lipo -info .build/universal/MouseStrideDaemon

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: >
          gh release create "${{ steps.version.outputs.tag }}"
          "MouseStride-${{ steps.version.outputs.tag }}.pkg"
          "MouseStrideDaemon-${{ steps.version.outputs.tag }}.zip"
          --title "MouseStride ${{ steps.version.outputs.tag }}"
          --generate-notes

      - name: Clean up keychain
        if: always()
        run: security delete-keychain build.keychain || true
