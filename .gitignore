# Swift
.build/
.swiftpm/
*.app/
*.xcodeproj/
xcuserdata/

# Rust
target/

# macOS
.DS_Store
*.dmg

# Playwright MCP cache (screenshots, console logs, page snapshots)
.playwright-mcp/

# Release artifacts (produced by `make zip` / `make pkg` locally)
MouseStrideDaemon-*.zip
MouseStride-*.pkg

# Credentials (safety net — should never be in the repo)
*.p12
*.pfx
*.key
*.cer
*.entitlements.local
